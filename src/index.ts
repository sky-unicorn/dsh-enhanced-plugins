import type { Context } from '@deepseek-ai/cordis'

export const name = 'enhanced-plugins-client-anchor'
export const inject: string[] = []

/** Package-root Loader entry used by DSH Client-module discovery. */
export function apply(_ctx: Context): void {}
