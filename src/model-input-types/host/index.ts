import type { Context } from '@deepseek-ai/cordis'

export const name = 'model-input-types-client-anchor'
export const inject: string[] = []

/** Host-side anchor that makes this client-only feature visible to DSH Client discovery. */
export function apply(_ctx: Context): void {}
